/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typedRoutes: false,
  // The dev overlay badge sits bottom-left, on top of the sidebar's signed-in name.
  devIndicators: false,
};

export default nextConfig;
