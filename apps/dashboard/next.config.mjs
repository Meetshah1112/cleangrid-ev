/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typedRoutes: false,
  // The dev overlay badge sits bottom-left, on top of the scenery and its legends.
  devIndicators: false,
};

export default nextConfig;
